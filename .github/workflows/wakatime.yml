name: WakaTime Status Update

on:
  schedule:
    - cron: "0 0 * * *" # 每天凌晨更新
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Fetch and Process WakaTime Data
        env:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
        # 關鍵就在下面這一行，指定用 python 執行
        shell: python
        run: |
          import os
          import requests
          import base64
          import re

          # 1. 抓取 WakaTime 統計資料
          api_key = os.getenv('WAKATIME_API_KEY')
          if not api_key:
              print("Error: WAKATIME_API_KEY is missing (´д｀)")
              exit(1)

          encoded_key = base64.b64encode(api_key.encode()).decode()
          url = "https://wakatime.com/api/v1/users/current/stats/last_7_days"
          
          headers = {
              "Authorization": f"Basic {encoded_key}"
          }
          
          response = requests.get(url, headers=headers)
          data = response.json().get('data', {})

          # 2. 格式化輸出內容
          if not data:
              content = "No WakaTime data found for the last 7 days (´･_･`)"
          else:
              languages = data.get('languages', [])[:5]
              content = "#### 📊 過去一週的開發數據\n\n```text\n"
              for lang in languages:
                  name = lang['name'].ljust(12)
                  percent = lang['percent']
                  bar_length = int(percent / 4)
                  bar = '█' * bar_length + '░' * (25 - bar_length)
                  content += f"{name} {bar} {percent:>5.2f} %\n"
              content += "```\n"

          # 3. 讀取並更新 README
          with open('README.md', 'r', encoding='utf-8') as f:
              readme = f.read()

          pattern = r"<!-- WAKATIME_START -->.*?<!-- WAKATIME_END -->"
          replacement = f"<!-- WAKATIME_START -->\n{content}\n<!-- WAKATIME_END -->"
          
          if re.search(pattern, readme, re.DOTALL):
              new_readme = re.sub(pattern, replacement, readme, flags=re.DOTALL)
          else:
              new_readme = readme + "\n\n" + replacement

          with open('README.md', 'w', encoding='utf-8') as f:
              f.write(new_readme)

      - name: Commit and Push
        run: |
          git config --global user.name "DataAnalysisBot"
          git config --global user.email "bot@github.com"
          git add README.md
          # 如果內容沒變動，git commit 會報錯，所以加個 || exit 0 忽略它
          git commit -m "Update WakaTime stats (´∀`)b" || exit 0
          git push